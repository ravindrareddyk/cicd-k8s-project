apiVersion: v1
kind: Service
metadata:
  name: cicd-demo-service
spec:
  type: NodePort
  selector:
    app: cicd-demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30090